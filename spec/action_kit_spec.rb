require 'spec_helper'

describe ActionKit do
  it 'has a version number' do
    expect(ActionKit::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
